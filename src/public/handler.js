/**
 * testEndpoint
 * Created for testing the project boilerplate, this endpoint should
 * be deleted or mutated into another endpoint
 *
 * @return {Object} - it returns an HTTP response.
 */
export async function getStatus() {
  return {statusCode: 200};
}
